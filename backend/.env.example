# Database
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/student_ai_assistant

# Google OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Canvas (user-provided per account, but app needs to know the pattern)
# Individual users will provide their own Canvas URL + token
